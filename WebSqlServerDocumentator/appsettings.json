{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "SqlDocumentatorConfiguration": {
    "Prefix": "Test",
    "Servers": [
      {
        "Name": "localhost",
        "DisplayName": "servidor local",
        "Description": "Localhost's desciption"
      },
      {
        "Name": "localhost",
        "DisplayName": "servidor local 2",
        "Description": "Localhost's desciption 2"
      }
    ]
  }
}
